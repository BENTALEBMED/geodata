<form method="post" action="{{ path('mere_delete', {'id': mere.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mere.id) }}">
    <button class="btn btn-outline-success">Supprimer</button>
</form>
