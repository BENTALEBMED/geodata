{% extends 'base.html.twig' %}

{% block title %}Cercle{% endblock %}

{% block body %}
    <h1>Cercle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cercle.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ cercle.nom }}</td>
            </tr>
            <tr>
                <th>Commune</th>
                <td>{{ cercle.commune }}</td>
            </tr>
            <tr>
                <th>Region</th>
                <td>{{ cercle.region }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cercle_index') }}">back to list</a>

    <a href="{{ path('cercle_edit', {'id': cercle.id}) }}">edit</a>

    {{ include('cercle/_delete_form.html.twig') }}
{% endblock %}
