{{ form_start(form) }}
    
    
      {# Génération manuelle et éclatée : #}
        <div class="row mb-3">
            {# Génération du label. #}
            {{ form_label(form.smi, "SMI :", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis. #}
            {{ form_errors(form.smi) }}
            <div class="col-sm-2">
                {# Génération de l'input. #}
                {{ form_widget(form.smi, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>
         <div class="row mb-3">
            {# Génération du label. #}
            {{ form_label(form.nom, "Nom :", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis. #}
            {{ form_errors(form.nom) }}
            <div class="col-sm-3">
                {# Génération de l'input. #}
                {{ form_widget(form.nom, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>
        <div class="row mb-3">
            {# Génération du label. #}
            {{ form_label(form.prenom, "Prénom :", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis. #}
            {{ form_errors(form.prenom) }}
            <div class="col-sm-3">
                {# Génération de l'input. #}
                {{ form_widget(form.prenom, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>

         <div class="row mb-3">
            {# Génération du label. #}
            {{ form_label(form.sexe, "Sexe :", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis. #}
            {{ form_errors(form.sexe) }}
            <div class="col-sm-1">
                {# Génération de l'input. #}
                {{ form_widget(form.sexe, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>

        <div class="row mb-3">
            {# Génération du label. #}
            {{ form_label(form.dateNaissance, "Date Naissance :", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis. #}
            {{ form_errors(form.dateNaissance) }}
            <div class="col-sm-2">
                {# Génération de l'input. #}
                {{ form_widget(form.dateNaissance, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>

        <div class="row mb-3">
            {# Génération du label. #}
            {{ form_label(form.cinPereMere, "CIN Mère/Père :", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis. #}
            {{ form_errors(form.cinPereMere) }}
            <div class="col-sm-2">
                {# Génération de l'input. #}
                {{ form_widget(form.cinPereMere, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>
        <div class="row mb-3">
            {# Génération du label. #}
            {{ form_label(form.accouchement, "Accouchement :", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis. #}
            {{ form_errors(form.accouchement) }}
            <div class="col-sm-2">
                {# Génération de l'input. #}
                {{ form_widget(form.accouchement, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>
        <div class="row mb-3">
            {# Génération du label. #}
            {{ form_label(form.durreeAllaitement, "Durrée Allaitement(en mois) :", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis. #}
            {{ form_errors(form.durreeAllaitement) }}
            <div class="col-sm-1">
                {# Génération de l'input. #}
                {{ form_widget(form.durreeAllaitement, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>
        <div class="row mb-3">
            {# Génération du label. #}
            {{ form_label(form.douars, "Nom Douar :", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis. #}
            {{ form_errors(form.douars) }}
            <div class="col-sm-2">
                {# Génération de l'input. #}
                {{ form_widget(form.douars, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>
       
        <div class="row mb-3">
            {# Génération du label. #}
            {{ form_label(form.meres, "Nom Mère (Parent)", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis. #}
            {{ form_errors(form.meres) }}
            <div class="col-sm-2">
                {# Génération de l'input. #}
                {{ form_widget(form.meres, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>
         <div class="row mb-3">
            {# Génération du label. #}
            {{ form_label(form.datedernieracc, "Date Dernier Accouchement", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis. #}
            {{ form_errors(form.datedernieracc) }}
            <div class="col-sm-2">
                {# Génération de l'input. #}
                {{ form_widget(form.datedernieracc, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>
       <div class="row mb-3">
            {# Génération du label. #}
            {{ form_label(form.nbrcpn, "Nombre CPN :", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis. #}
            {{ form_errors(form.nbrcpn) }}
            <div class="col-sm-1">
                {# Génération de l'input. #}
                {{ form_widget(form.nbrcpn, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>
        <div class="row mb-3">
        
            {# Génération du label. #}
            {{ form_label(form.fer, "Prise du Fer pendant les 6 premiers mois  (Oui / Non)", {'label_attr': {'class': 'col-sm-2 control-label '}}) }}
            {# Affichage des erreurs pour ce champ précis. #}
            {{ form_errors(form.fer) }}
            <div class="col-sm-1">
                {# Génération de l'input. #}
                {{ form_widget(form.fer, {'attr': {'class': 'form-control'}}) }}
            </div>
         
        </div>

        <div class="row mb-3">
        
            {# Génération du label. #}
            {{ form_label(form.vitamineD, "Prise vitamine D pendant les 6 premiers mois  (Oui / Non)", {'label_attr': {'class': 'col-sm-2 control-label '}}) }}
            {# Affichage des erreurs pour ce champ précis. #}
            {{ form_errors(form.vitamineD) }}
            <div class="col-sm-1">
                {# Génération de l'input. #}
                {{ form_widget(form.vitamineD, {'attr': {'class': 'form-control '}}) }}
            </div>
        
        </div>
        <div class="row mb-3">
       
            {# Génération du label. #}
            {{ form_label(form.acideFolique, "Prise Acide Folique pendant les 6 premiers mois  (Oui / Non)", {'label_attr': {'class': 'col-sm-2 control-label '}}) }}
            {# Affichage des erreurs pour ce champ précis. #}
            {{ form_errors(form.acideFolique) }}
            <div class="col-sm-1">
                {# Génération de l'input. #}
                {{ form_widget(form.acideFolique, {'attr': {'class': 'form-control '}}) }}
            </div>
           
        </div>
        <div class="row mb-3">
            {# Génération du label. #}
            {{ form_label(form.suppNutri, "Suppléments nutritionnels Grossesse : ", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis. #}
            {{ form_errors(form.suppNutri) }}
            <div class="col-sm-5">
                {# Génération de l'input. #}
                {{ form_widget(form.suppNutri, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>
        <div class="row mb-3">
            {# Génération du label. #}
            {{ form_label(form.motifDGross, "Motif de danger si la grossesse était à risque :", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis. #}
            {{ form_errors(form.motifDGross) }}
            <div class="col-sm-5">
                {# Génération de l'input. #}
                {{ form_widget(form.motifDGross, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>

       
    
    <button class="btn btn-outline-success">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
