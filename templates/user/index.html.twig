{% extends 'base.html.twig' %}

{% block title %}Utilisateurs{% endblock %}

{% block body %}
<div class="card">
<div class="card-body">
    <h5 class="card-title">Mise à jours Utilisateur</h5>

    <section class="section">
    <div class="row">
    <div class="col-lg-12">
    

    <table class="table  table-striped">
    <thead>
    <tr>
    <th>Id</th>
    <th>Cordial</th>
    <th>Nom</th>
    <th>Prenom</th>
    <th>Fonction</th>
    <th>Telephone</th>
    <th>Email</th>
    <th>Centre</th>
    <th>Compte</th>   
    <th>Role</th>
    <th>actions</th>
    </tr>
    </thead>
    <tbody>
    {% for user in users %}
    <tr>
    <td>{{ user.id }}</td>
    <td>{{ user.cordial }}</td>
    <td>{{ user.nom }}</td>
    <td>{{ user.prenom }}</td>
    <td>{{ user.fonction }}</td>
    <td>{{ user.telephone }}</td>
    <td>{{ user.email }}</td>
    <td>{{ user.centre }}</td>
    <td>{{ user.compte }}</td>    
    <td>{{ user.role }}</td>
    <td>
        <a class="btn btn-outline-success"  href="{{ path('user_show', {'id': user.id}) }}">Afficher</a>
        <a class="btn btn-outline-danger" href="{{ path('user_edit', {'id': user.id}) }}">Modifier</a>
    </td>
    </tr>
    {% else %}
    <tr>
    <td colspan="12">no records found</td>
    </tr>
    {% endfor %}
    </tbody>
    </table>

   
    </div>
    </div>
    </section>
</div>
</div>

{% endblock %}
